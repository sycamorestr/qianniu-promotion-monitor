---
name: qianniu-promotion-monitor
description: 巡检千牛店铺的阿里妈妈全站推和关键词推广，授权后暂停；异常交由 agent 核验恢复，最终按待暂停清单和已暂停结果发送企业微信汇总。
---

# 千牛 / 阿里妈妈推广巡检

这个 Skill 将用户意图转换成巡检、提醒或执行任务。常规流程使用 runner；发生错误由当前 agent 诊断和恢复。保持页面身份与写前复核，不手工猜测接口或盲目重放暂停请求。

## Agent 流程

1. 从本目录读取 `config.json`。首次安装、迁移或依赖异常时先阅读 [README.md](README.md)，必要时运行 `node scripts/check.mjs`。
2. 按用户意图选择模式；环境提供运行器时调用对应入口，否则由 agent 使用可用工具完成同一契约：
   - 仅查看、检查或生成报告：`node runner.mjs --mode report`（默认，只写本地审计）。
   - 用户明确要求发企业微信提醒、但未授权暂停：`node runner.mjs --mode dry-run`。
   - 用户明确授权暂停符合规则的计划：`node runner.mjs --mode execute`，正常结束后发送最终结果；存在未解决问题时先交回 agent，不提前通知。
3. 没有明确暂停授权时不得使用 `execute`。安装、迁移、生成清单或讨论方案都不等于授权；已有明确授权时无需重复确认。
4. 读取审计。出现 `recovery.status=awaiting-agent`、`notification.status=awaiting-agent`、退出码 2 或未完成检查点时，当前 agent 必须继续接手，依照 [异常恢复](references/recovery.md) 检查原因、只读核验，并在安全条件满足时重试；不把 runner 返回当作任务结束。恢复通常最多两轮，每计划最多一次恢复写入。用户对本轮 execute 的授权包含符合原规则的恢复重试，无需重复确认。
5. 恢复完成或确实无法继续时，执行 `node runner.mjs --finalize RUN_ID`，只发送该轮最终审计；不为补发消息重新扫描。已经 sent 的通知不会重发，sending/failed 先检查已发送情况。

`dry-run` 和 `execute` 需要当前进程提供 `WECHAT_WEBHOOK_URL`。只检查是否存在，不打印值。Cookie、CSRF、Webhook、登录凭据和浏览器 profile 不得写入 Skill、配置示例或分享包。

## 行为契约

- 按配置的店铺顺序处理，并以页面返回的实际店铺身份为准；身份无法确认时不写入。
- 规则和阈值以 `config.json` 为准，只处理仍处于活动状态且指标完整的推广计划。
- 登录态失效是 profile 级终止条件：识别到登录页、未登录或会话过期后，不在该 profile 上继续扫描重试、暂停写入或重复回查；保留待核验记录，重新登录后再恢复。普通页面未就绪、网络或超时错误仍按有限次数处理。
- 暂停写前核对身份与最新指标。执行后和恢复时只按本轮待暂停清单的计划 ID 核验，不重扫全店，也不读取无关推广类型。每个目标必须有明确状态；查询不到、筛选未生效或身份不匹配均为待核验，不能以列表缺失认定暂停。确认旧页面操作已结束、计划仍活动且仍符合阈值后，agent 才可经恢复入口重试。未决写入只阻断同一推广计划的后续写入；同店其他计划仍按各自身份和最新指标串行复核后继续处理。无法确认旧操作结束时仅核验同一计划，不强行重试。
- “待暂停”是本轮筛选出的全部需要执行暂停的记录；“已暂停”是执行后最新核验确认停用的子集。两者是执行前清单与执行后结果，不能把失败数量或剩余数量称作待暂停。不使用“推广未暂停”及其计数。无法确认的记录在待暂停清单注明“待核验”，确认仍活动的注明“仍在推广”，指标改善的不再执行。
- 企业微信等整轮执行和 agent 恢复结束后统一发送。先发按店铺分组的低 ROI 巡检结果，最后单独发送集中“暂停操作汇总”，按店铺列“待暂停”和“已暂停”两阶段清单。零值不显示；无候选时说明无符合暂停条件的推广。用 `# / ## / ###` 层级、`info` 绿色已暂停、`warning` 橙色待暂停、`comment` 灰色指标；不展示 trace、异常原文、归因等技术信息。
- 页面命令、重试、审计、检查点、消息分包等实现细节由 `runner.mjs` 和 `adapters/` 维护；agent 不应绕过入口直接拼接内部接口。

## 交付与维护

结果写入 `audit/`。`report` 不发通知；通知或执行失败时保留审计并明确告知用户。修改适配器或运行器后，在不连接真实店铺的前提下运行：

```text
node --test
node scripts/check.mjs
```

分享前使用 `npm pack --ignore-scripts`；白名单不会包含本机 `config.json`、`audit/`、登录态或环境变量。离线测试只能验证本地契约，不能替代授权的线上只读核验。
